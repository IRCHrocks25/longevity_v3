[phases.install]
cmds = [
  "rm -rf node_modules package-lock.json",
  "npm install --include=dev",
  "chmod +x node_modules/.bin/* || true"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

